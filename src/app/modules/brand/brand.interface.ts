import { Model } from 'mongoose';

export type IBrand = {
  fuelCategory: string;
  carType: string;
  transmission: string;
  seatNumber: number;
  interiorColor: string;
  pricePerDay: string;
  logo: string;
};

